//Exercise 1: Run the code using two different distances: Euclidean and Manhattan distances.
/*
 Parameters: 
   a: With two components: a[0] and a[1], 'x' and 'y' coordinates
   b: With two components: a[0] and a[1], 'x' and 'y' coordinates
 Returns: Distance value.
*/
function distancefunction(a, b) {
            var dx = b[0] - a[0];
            var dy = b[1] - a[1];
            return 0;//Replace with the calculated distance
        }
